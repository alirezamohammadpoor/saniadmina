<footer
  class="footer"
  data-section-id="{{ section.id }}"
  data-section-type="footer"
>
  <div class="container py-10 lg:py-10">
    <div class="flex flex-col gap-6 lg:flex-row lg:gap-20">
      {%- comment -%} Customer Care {%- endcomment -%}
      <div class="flex flex-col gap-1">
        <span class="text-label text-fg">Customer Care</span>
        <a href="mailto:{{ section.settings.email | default: 'info@saniadmina.com' }}" class="text-[14px] text-muted">
          {{ section.settings.email | default: 'info@saniadmina.com' }}
        </a>
      </div>

      {%- comment -%} Menu blocks {%- endcomment -%}
      {% for block in section.blocks %}
        <div class="flex flex-col gap-1" {{ block.shopify_attributes }}>
          <span class="text-label text-fg">
            {{ block.settings.title }}
          </span>

          {%- if block.settings.menu != blank -%}
            <nav aria-label="{{ block.settings.title }}">
              <ul class="flex flex-col gap-1">
                {%- for link in block.settings.menu.links -%}
                  <li>
                    <a
                      href="{{ link.url }}"
                      class="text-[14px] text-muted"
                      {% if link.current %}aria-current="page"{% endif %}
                    >
                      {{ link.title }}
                    </a>
                  </li>
                {%- endfor -%}
              </ul>
            </nav>
          {%- endif -%}
        </div>
      {% endfor %}
    </div>

    {%- comment -%} Copyright {%- endcomment -%}
    {% if section.settings.copyright_text != blank %}
      {%- liquid
        assign current_year = 'now' | date: '%Y'
        assign shop_name = shop.name
      %}
      <div class="mt-10 text-[12px] text-muted">
        {{ section.settings.copyright_text | replace: '$year', current_year | replace: '$shop', shop_name }}
      </div>
    {% endif %}

  </div>

  {%- comment -%} Large brand text — edge to edge {%- endcomment -%}
  <div class="text-[15vw] font-light leading-none text-fg overflow-hidden text-center">
    SANIA D'MINA
  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "max_blocks": 4,
  "settings": [
    {
      "type": "text",
      "id": "email",
      "label": "Customer Care Email",
      "default": "info@saniadmina.com"
    },
    {
      "type": "header",
      "content": "Copyright"
    },
    {
      "type": "textarea",
      "id": "copyright_text",
      "label": "Copyright Text",
      "info": "Use $year for current year. Use $shop for shop name.",
      "default": "© $year Sania D'Mina"
    }
  ],
  "blocks": [
    {
      "type": "menu",
      "name": "Menu",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Menu"
        }
      ]
    }
  ]
}
{% endschema %}
